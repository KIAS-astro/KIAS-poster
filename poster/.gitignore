# local environments
.venv/
__pycache__/
*.pyc

# dev-only blend-variant renderer (not part of the final poster)
make_variants.py

# generated output — recreate with make_poster.py
drafts/

# downloadable + derived images — run fetch_images.py to recreate.
# everything in images/ is ignored except the stored-in-repo assets listed here.
images/*
!images/KIAS-banner.png
